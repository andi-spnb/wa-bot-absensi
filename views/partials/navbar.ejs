<nav class="navbar">
    <div class="container">
        <div class="navbar-brand">
            <a href="/">WA Bot Admin</a>
        </div>
        <% if (user) { %>
            <div class="navbar-menu">
                <a href="/" class="<%= path === '/' ? 'active' : '' %>">Dashboard</a>
                <a href="/api-keys" class="<%= path.startsWith('/api-keys') ? 'active' : '' %>">API Keys</a>
                <a href="/logs" class="<%= path.startsWith('/logs') ? 'active' : '' %>">Logs</a>
                <div class="dropdown">
                    <button class="btn-dropdown"><%= user.username %></button>
                    <div class="dropdown-content">
                        <a href="/profile">Profile</a>
                        <a href="/settings">Settings</a>
                        <a href="/logout">Logout</a>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
</nav>